import 'package:flutter/material.dart';
class viewAttendance extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Center(child: Text('View Attendance')),),);
  }
}
